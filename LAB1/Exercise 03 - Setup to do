<!DOCTYPE html>
<html lang="en">

  <head>
    <script src='https://d3js.org/d3.v7.min.js'></script>

  </head>



  <style>
    .paint {
      color: red
    }

  </style>

  <p><label>red</label><input type="checkbox" /></p>
  <p><label>yellow</label><input type="checkbox" /></p>
  <p id="col"><label>green</label><input type="checkbox" /></p>

  <body>
    <script>
    
    /*Exercise 3: Write a loop which adds 10 ‘div’ elements and sets the contents to the count value (i.e., 1, 2, 3, ...).  Also 
the color of the first 5 elements are red and the last 5 elements are green (lookup the attr(), property() and style() 
methods). */
    
    
    
      // give ten div 
      for (let i = 1; i <= 10; i++) {
      
        let newspan = d3.select("body").append("div").text(i);
				
        if (i < 6) {
          //ATTRIBUTE
          //up to 5 red  
          newspan.attr("class", "paint");
          
        } else {
          //STYLE
          //add a font colour green to the divison text selected
          newspan.style("color", "green")
          //PROPERTY
          // give a separate ID in order to be able to manipulate the 
          //check box 
          d3.select("#col").select("input").property("checked", true);
        }
    
      }
      //apply attribute with property for checked properties 
      d3.select("input").property("checked", true);

    </script>
  </body>

</html>
