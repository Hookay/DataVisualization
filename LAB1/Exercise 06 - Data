<!DOCTYPE html>
<html lang="en">

  <head>
    <script src='https://d3js.org/d3.v7.min.js'></script>

  </head>


  <body>

    <div></div>
    <div></div>
    <div></div>

    <script>
    /*Exercise 6: Modify the example below so the ‘otherdata’ contains an additional variable called color (print this color 
value out in the ‘text’ method. */
    
      // add color variable                
      let otherdata = [{
          name: 'test',
          val: 1,
          color: "red"
        },
        {
          name: 'other',
          val: 2,
          color: "brown"
        },
        {
          name: 'b',
          val: 3,
          color: "purple"
        }
      ];
      //print color variable in text method
      let paragraph = d3.select("body")
        .selectAll("div")
        .data(otherdata)
        .text(function(d, i) {
          console.log("d.name: " + d.name);
          console.log("d.val:  " + d.val);
          console.log("d.color:  " + d.color);
          console.log("i: " + i);
          console.log("this: " + this);

          return 'color: ' + d.color; // return value is used to set the 'text' 
        });

    </script>
  </body>

</html>
